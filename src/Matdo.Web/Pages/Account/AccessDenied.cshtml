@page
@{
    ViewData["Title"] = T["auth.accessDenied"];
}
<div class="auth-wrap">
    <div class="auth-card">
        <div class="logo"><icon name="x" size="26" /> Matdo</div>
        <h1>@T["auth.accessDenied"]</h1>
        <p class="sub">@T["auth.accessDeniedText"]</p>
        <a class="btn btn-primary" href="/">@T["auth.backHome"]</a>
    </div>
</div>
